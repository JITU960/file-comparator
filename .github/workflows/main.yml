name: Hello World

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      manual_trigger:
        description: 'Manually trigger the workflow'

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, world!"
